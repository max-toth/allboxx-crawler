$(document).ready(function(){

	// Init baron.js
	var scroll = baron({
		scroller: '.messages',
		root: $('.chat__messages'),
		bar: '.messages__scrollbar',
		barOnCls: 'chat__messages_scroll'
	});

});